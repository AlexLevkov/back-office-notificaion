# back-office-notificaion

## Description

## Features

- Form with `appId`, `appToken`, `title`, and `body` fields.
- Two-way data binding with `useState`.
- POST request handling with Axios.

## Installation
